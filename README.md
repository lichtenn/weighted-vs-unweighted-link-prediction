# weighted-vs-unweighted-link-prediction
Comparison of weighted and unweighted link prediction methods (Preferential Attachment, Shortest Path, Local Path, L3) in a bipartite network, evaluated both as standalone ranking scores and as features for supervised classifiers (Logistic Regression, KNN, XGBoost).
